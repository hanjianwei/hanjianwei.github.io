sudo: false
language: ruby
cache: bundler
rvm:
- 2.1.1
script: jekyll build --config _config.yml,_local.yml && htmlproof ./_site

branches:
  only:
  - master

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

notifications:
  email: false
