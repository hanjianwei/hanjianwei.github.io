sudo: false
language: ruby
cache: bundler
rvm:
- 2.1
script: bundle exec jekyll build --config _config.yml,_local.yml && bundle exec htmlproof ./_site

branches:
  only:
  - master

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

notifications:
  email: false
